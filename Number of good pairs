def numIdenticalPairs(self, nums: List[int]) -> int:
        l=len(nums)
        output=0
        for i in range(l-1):
            for j in range(i+1,l):
                if nums[i]==nums[j]:
                    output+=1
        return output
            
